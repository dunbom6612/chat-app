import React from 'react';
import PropTypes from 'prop-types';
import './chat-list.scss';

ChatList.propTypes = {};

function ChatList(props) {
  return (
    <div className="chat-list">
      <h1> CHAT LIST SCREEN</h1>
    </div>
  );
}

export default ChatList;
